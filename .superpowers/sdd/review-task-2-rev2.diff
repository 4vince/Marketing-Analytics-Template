# Review package: 89678fc..e989996f960c26c63e7732158b69e6bb26a19f38

## Commits
e989996 fix: handle db error and safe image cast e46ce15 feat: add storefront layout, home page, product grid

## Files changed
 .superpowers/sdd/progress.md              | Bin 0 -> 100 bytes  .superpowers/sdd/review-task-1.diff       |  10 ++  .superpowers/sdd/review-task-2.diff       |  10 ++  .superpowers/sdd/task-1-report.md         |  38 ++++++  .superpowers/sdd/task-2-brief.md          | 193 ++++++++++++++++++++++++++++++  .superpowers/sdd/task-2-report.md         |  27 +++++  src/app/layout.tsx                        |  21 ++++  src/app/page.tsx                          |  51 ++++++++  src/components/storefront/Footer.tsx      |   9 ++  src/components/storefront/Header.tsx      |  17 +++  src/components/storefront/ProductCard.tsx |  36 ++++++  src/components/storefront/ProductGrid.tsx |  19 +++  12 files changed, 431 insertions(+)

## Diff
diff --git a/.superpowers/sdd/progress.md b/.superpowers/sdd/progress.md new file mode 100644 index 0000000..856e40b Binary files /dev/null and b/.superpowers/sdd/progress.md differ diff --git a/.superpowers/sdd/review-task-1.diff b/.superpowers/sdd/review-task-1.diff new file mode 100644 index 0000000..cfe786f --- /dev/null +++ b/.superpowers/sdd/review-task-1.diff @@ -0,0 +1,10 @@ +# Review package: 89678fc80c941d04903f2abc8fcac79a993061f0..HEAD + +## Commits + + +## Files changed + + +## Diff + diff --git a/.superpowers/sdd/review-task-2.diff b/.superpowers/sdd/review-task-2.diff new file mode 100644 index 0000000..eb9f698 --- /dev/null +++ b/.superpowers/sdd/review-task-2.diff @@ -0,0 +1,10 @@ +# Review package: 89678fc..e46ce157a8e85906ea229367f8fcdc298cffb9cb + +## Commits +e46ce15 feat: add storefront layout, home page, product grid + +## Files changed + .superpowers/sdd/progress.md              | Bin 0 -> 100 bytes  .superpowers/sdd/review-task-1.diff       |  10 ++  .superpowers/sdd/task-1-report.md         |  38 ++++++  .superpowers/sdd/task-2-brief.md          | 193 ++++++++++++++++++++++++++++++  src/app/layout.tsx                        |  21 ++++  src/app/page.tsx                          |  45 +++++++  src/components/storefront/Footer.tsx      |   9 ++  src/components/storefront/Header.tsx      |  17 +++  src/components/storefront/ProductCard.tsx |  36 ++++++  src/components/storefront/ProductGrid.tsx |  19 +++  10 files changed, 388 insertions(+) + +## Diff +diff --git a/.superpowers/sdd/progress.md b/.superpowers/sdd/progress.md new file mode 100644 index 0000000..856e40b Binary files /dev/null and b/.superpowers/sdd/progress.md differ diff --git a/.superpowers/sdd/review-task-1.diff b/.superpowers/sdd/review-task-1.diff new file mode 100644 index 0000000..cfe786f --- /dev/null +++ b/.superpowers/sdd/review-task-1.diff @@ -0,0 +1,10 @@ +# Review package: 89678fc80c941d04903f2abc8fcac79a993061f0..HEAD + +## Commits + + +## Files changed + + +## Diff + diff --git a/.superpowers/sdd/task-1-report.md b/.superpowers/sdd/task-1-report.md index e69de29..a189247 100644 --- a/.superpowers/sdd/task-1-report.md +++ b/.superpowers/sdd/task-1-report.md @@ -0,0 +1,38 @@ +# Task 1 Report: Scaffold Next.js + Prisma + Database Schema + +## What was implemented +- Created Next.js 14 project with TypeScript, Tailwind CSS, and Prisma +- Set up project structure: package.json, tsconfig.json, next.config.js, tailwind.config.ts, postcss.config.js +- Created .env.example with all required environment variables +- Defined complete Prisma schema with 7 models: User, Product, Order, AnalysisResult, QuarterlyReport, PageContent, ChatConversation, ChatMessage +- Created Prisma client singleton in src/lib/prisma.ts +- Created globals.css with Tailwind directives +- Added .gitignore + +## Test results +- `npm install` G�� succeeded (153 packages) +- `npx prisma generate` G�� succeeded (Prisma Client v5.22.0) +- `npx next build` G�� succeeded (compiled successfully, built /404 static page) + +## Files changed +- `.env.example` +- `.gitignore` +- `next-env.d.ts` (auto-generated by Next) +- `next.config.js` +- `package-lock.json` +- `package.json` +- `postcss.config.js` +- `prisma/schema.prisma` +- `src/app/globals.css` +- `src/lib/prisma.ts` +- `tailwind.config.ts` +- `tsconfig.json` + +## Self-review findings +- All files match the task brief exactly +- Build compiles with zero errors +- Prisma client generated successfully +- No .env file created (only .env.example) G�� user needs to copy and fill in real values + +## Issues +- None diff --git a/.superpowers/sdd/task-2-brief.md b/.superpowers/sdd/task-2-brief.md new file mode 100644 index 0000000..27433f8 --- /dev/null +++ b/.superpowers/sdd/task-2-brief.md @@ -0,0 +1,193 @@ +### Task 2: Storefront Layout + Home Page + +**Files:** +- Create: `src/app/layout.tsx` +- Create: `src/app/page.tsx` +- Create: `src/components/storefront/Header.tsx` +- Create: `src/components/storefront/Footer.tsx` + +**Interfaces:** +- Produces: Root layout wrapping all pages with Header + Footer +- Produces: Home page with hero and featured products section + +- [ ] **Step 1: Create root layout src/app/layout.tsx** + +```tsx +import type { Metadata } from "next"; +import "./globals.css"; +import Header from "@/components/storefront/Header"; +import Footer from "@/components/storefront/Footer"; + +export const metadata: Metadata = { +  title: "Store", +  description: "E-commerce template", +}; + +export default function RootLayout({ children }: { children: React.ReactNode }) { +  return ( +    <html lang="en"> +      <body className="min-h-screen flex flex-col"> +        <Header /> +        <main className="flex-1">{children}</main> +        <Footer /> +      </body> +    </html> +  ); +} +``` + +- [ ] **Step 2: Create Header src/components/storefront/Header.tsx** + +```tsx +import Link from "next/link"; + +export default function Header() { +  return ( +    <header className="border-b"> +      <div className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between"> +        <Link href="/" className="text-xl font-bold">Store</Link> +        <nav className="flex gap-6"> +          <Link href="/products">Products</Link> +          <Link href="/about">About</Link> +          <Link href="/contact">Contact</Link> +          <Link href="/cart">Cart</Link> +        </nav> +      </div> +    </header> +  ); +} +``` + +- [ ] **Step 3: Create Footer src/components/storefront/Footer.tsx** + +```tsx +export default function Footer() { +  return ( +    <footer className="border-t py-8 mt-auto"> +      <div className="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500"> +        &copy; {new Date().getFullYear()} Store. All rights reserved. +      </div> +    </footer> +  ); +} +``` + +- [ ] **Step 4: Create Home page src/app/page.tsx** + +```tsx +import Link from "next/link"; +import ProductGrid from "@/components/storefront/ProductGrid"; +import { prisma } from "@/lib/prisma"; + +export default async function HomePage() { +  const products = await prisma.product.findMany({ +    where: { status: "active" }, +    take: 8, +    orderBy: { createdAt: "desc" }, +  }); + +  return ( +    <div> +      <section className="bg-gradient-to-br from-primary-50 to-white py-20"> +        <div className="max-w-7xl mx-auto px-4 text-center"> +          <h1 className="text-4xl font-bold mb-4">Welcome to Our Store</h1> +          <p className="text-lg text-gray-600 mb-8 max-w-2xl mx-auto"> +            Discover our curated collection of products. +          </p> +          <Link +            href="/products" +            className="inline-block bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-700" +          > +            Shop Now +          </Link> +        </div> +      </section> +      <section className="max-w-7xl mx-auto px-4 py-16"> +        <h2 className="text-2xl font-bold mb-8">Featured Products</h2> +        <ProductGrid products={products} /> +      </section> +    </div> +  ); +} +``` + +- [ ] **Step 5: Create ProductCard src/components/storefront/ProductCard.tsx** + +```tsx +import Link from "next/link"; + +interface ProductCardProps { +  product: { +    id: string; +    name: string; +    slug: string; +    price: number; +    images: string[]; +    category: string; +  }; +} + +export default function ProductCard({ product }: ProductCardProps) { +  return ( +    <Link +      href={`/products/${product.slug}`} +      className="group border rounded-lg overflow-hidden hover:shadow-lg transition" +    > +      <div className="aspect-square bg-gray-100"> +        {product.images[0] && ( +          <img +            src={product.images[0]} +            alt={product.name} +            className="w-full h-full object-cover group-hover:scale-105 transition" +          /> +        )} +      </div> +      <div className="p-4"> +        <p className="text-sm text-gray-500 mb-1">{product.category}</p> +        <h3 className="font-semibold">{product.name}</h3> +        <p className="text-primary-600 font-bold mt-1">${(product.price / 100).toFixed(2)}</p> +      </div> +    </Link> +  ); +} +``` + +- [ ] **Step 6: Create ProductGrid src/components/storefront/ProductGrid.tsx** + +```tsx +import ProductCard from "./ProductCard"; + +interface ProductGridProps { +  products: Array<{ +    id: string; name: string; slug: string; price: number; +    images: string[]; category: string; +  }>; +} + +export default function ProductGrid({ products }: ProductGridProps) { +  if (products.length === 0) { +    return <p className="text-gray-500">No products yet.</p>; +  } +  return ( +    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"> +      {products.map((p) => <ProductCard key={p.id} product={p} />)} +    </div> +  ); +} +``` + +- [ ] **Step 7: Verify build** + +```bash +cd D:\ecommerce-template && npx next build 2>&1 | findstr /V "Info" +``` + +- [ ] **Step 8: Commit** + +```bash +git add . && git commit -m "feat: add storefront layout, home page, product grid" +``` + +--- + + diff --git a/src/app/layout.tsx b/src/app/layout.tsx new file mode 100644 index 0000000..5454733 --- /dev/null +++ b/src/app/layout.tsx @@ -0,0 +1,21 @@ +import type { Metadata } from "next"; +import "./globals.css"; +import Header from "@/components/storefront/Header"; +import Footer from "@/components/storefront/Footer"; + +export const metadata: Metadata = { +  title: "Store", +  description: "E-commerce template", +}; + +export default function RootLayout({ children }: { children: React.ReactNode }) { +  return ( +    <html lang="en"> +      <body className="min-h-screen flex flex-col"> +        <Header /> +        <main className="flex-1">{children}</main> +        <Footer /> +      </body> +    </html> +  ); +} diff --git a/src/app/page.tsx b/src/app/page.tsx new file mode 100644 index 0000000..e1029c0 --- /dev/null +++ b/src/app/page.tsx @@ -0,0 +1,45 @@ +import Link from "next/link"; +import ProductGrid from "@/components/storefront/ProductGrid"; +import { prisma } from "@/lib/prisma"; + +export const dynamic = "force-dynamic"; + +export default async function HomePage() { +  const raw = await prisma.product.findMany({ +    where: { status: "active" }, +    take: 8, +    orderBy: { createdAt: "desc" }, +  }); + +  const products = raw.map((p) => ({ +    id: p.id, +    name: p.name, +    slug: p.slug, +    price: p.price, +    images: p.images as string[], +    category: p.category, +  })); + +  return ( +    <div> +      <section className="bg-gradient-to-br from-primary-50 to-white py-20"> +        <div className="max-w-7xl mx-auto px-4 text-center"> +          <h1 className="text-4xl font-bold mb-4">Welcome to Our Store</h1> +          <p className="text-lg text-gray-600 mb-8 max-w-2xl mx-auto"> +            Discover our curated collection of products. +          </p> +          <Link +            href="/products" +            className="inline-block bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-700" +          > +            Shop Now +          </Link> +        </div> +      </section> +      <section className="max-w-7xl mx-auto px-4 py-16"> +        <h2 className="text-2xl font-bold mb-8">Featured Products</h2> +        <ProductGrid products={products} /> +      </section> +    </div> +  ); +} diff --git a/src/components/storefront/Footer.tsx b/src/components/storefront/Footer.tsx new file mode 100644 index 0000000..161b3f0 --- /dev/null +++ b/src/components/storefront/Footer.tsx @@ -0,0 +1,9 @@ +export default function Footer() { +  return ( +    <footer className="border-t py-8 mt-auto"> +      <div className="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500"> +        &copy; {new Date().getFullYear()} Store. All rights reserved. +      </div> +    </footer> +  ); +} diff --git a/src/components/storefront/Header.tsx b/src/components/storefront/Header.tsx new file mode 100644 index 0000000..46f5268 --- /dev/null +++ b/src/components/storefront/Header.tsx @@ -0,0 +1,17 @@ +import Link from "next/link"; + +export default function Header() { +  return ( +    <header className="border-b"> +      <div className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between"> +        <Link href="/" className="text-xl font-bold">Store</Link> +        <nav className="flex gap-6"> +          <Link href="/products">Products</Link> +          <Link href="/about">About</Link> +          <Link href="/contact">Contact</Link> +          <Link href="/cart">Cart</Link> +        </nav> +      </div> +    </header> +  ); +} diff --git a/src/components/storefront/ProductCard.tsx b/src/components/storefront/ProductCard.tsx new file mode 100644 index 0000000..1ce0e06 --- /dev/null +++ b/src/components/storefront/ProductCard.tsx @@ -0,0 +1,36 @@ +import Link from "next/link"; + +interface ProductCardProps { +  product: { +    id: string; +    name: string; +    slug: string; +    price: number; +    images: string[]; +    category: string; +  }; +} + +export default function ProductCard({ product }: ProductCardProps) { +  return ( +    <Link +      href={`/products/${product.slug}`} +      className="group border rounded-lg overflow-hidden hover:shadow-lg transition" +    > +      <div className="aspect-square bg-gray-100"> +        {product.images[0] && ( +          <img +            src={product.images[0]} +            alt={product.name} +            className="w-full h-full object-cover group-hover:scale-105 transition" +          /> +        )} +      </div> +      <div className="p-4"> +        <p className="text-sm text-gray-500 mb-1">{product.category}</p> +        <h3 className="font-semibold">{product.name}</h3> +        <p className="text-primary-600 font-bold mt-1">${(product.price / 100).toFixed(2)}</p> +      </div> +    </Link> +  ); +} diff --git a/src/components/storefront/ProductGrid.tsx b/src/components/storefront/ProductGrid.tsx new file mode 100644 index 0000000..10232c1 --- /dev/null +++ b/src/components/storefront/ProductGrid.tsx @@ -0,0 +1,19 @@ +import ProductCard from "./ProductCard"; + +interface ProductGridProps { +  products: Array<{ +    id: string; name: string; slug: string; price: number; +    images: string[]; category: string; +  }>; +} + +export default function ProductGrid({ products }: ProductGridProps) { +  if (products.length === 0) { +    return <p className="text-gray-500">No products yet.</p>; +  } +  return ( +    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"> +      {products.map((p) => <ProductCard key={p.id} product={p} />)} +    </div> +  ); +} diff --git a/.superpowers/sdd/task-1-report.md b/.superpowers/sdd/task-1-report.md index e69de29..a189247 100644 --- a/.superpowers/sdd/task-1-report.md +++ b/.superpowers/sdd/task-1-report.md @@ -0,0 +1,38 @@ +# Task 1 Report: Scaffold Next.js + Prisma + Database Schema + +## What was implemented +- Created Next.js 14 project with TypeScript, Tailwind CSS, and Prisma +- Set up project structure: package.json, tsconfig.json, next.config.js, tailwind.config.ts, postcss.config.js +- Created .env.example with all required environment variables +- Defined complete Prisma schema with 7 models: User, Product, Order, AnalysisResult, QuarterlyReport, PageContent, ChatConversation, ChatMessage +- Created Prisma client singleton in src/lib/prisma.ts +- Created globals.css with Tailwind directives +- Added .gitignore + +## Test results +- `npm install` G현 succeeded (153 packages) +- `npx prisma generate` G현 succeeded (Prisma Client v5.22.0) +- `npx next build` G현 succeeded (compiled successfully, built /404 static page) + +## Files changed +- `.env.example` +- `.gitignore` +- `next-env.d.ts` (auto-generated by Next) +- `next.config.js` +- `package-lock.json` +- `package.json` +- `postcss.config.js` +- `prisma/schema.prisma` +- `src/app/globals.css` +- `src/lib/prisma.ts` +- `tailwind.config.ts` +- `tsconfig.json` + +## Self-review findings +- All files match the task brief exactly +- Build compiles with zero errors +- Prisma client generated successfully +- No .env file created (only .env.example) G현 user needs to copy and fill in real values + +## Issues +- None diff --git a/.superpowers/sdd/task-2-brief.md b/.superpowers/sdd/task-2-brief.md new file mode 100644 index 0000000..27433f8 --- /dev/null +++ b/.superpowers/sdd/task-2-brief.md @@ -0,0 +1,193 @@ +### Task 2: Storefront Layout + Home Page + +**Files:** +- Create: `src/app/layout.tsx` +- Create: `src/app/page.tsx` +- Create: `src/components/storefront/Header.tsx` +- Create: `src/components/storefront/Footer.tsx` + +**Interfaces:** +- Produces: Root layout wrapping all pages with Header + Footer +- Produces: Home page with hero and featured products section + +- [ ] **Step 1: Create root layout src/app/layout.tsx** + +```tsx +import type { Metadata } from "next"; +import "./globals.css"; +import Header from "@/components/storefront/Header"; +import Footer from "@/components/storefront/Footer"; + +export const metadata: Metadata = { +  title: "Store", +  description: "E-commerce template", +}; + +export default function RootLayout({ children }: { children: React.ReactNode }) { +  return ( +    <html lang="en"> +      <body className="min-h-screen flex flex-col"> +        <Header /> +        <main className="flex-1">{children}</main> +        <Footer /> +      </body> +    </html> +  ); +} +``` + +- [ ] **Step 2: Create Header src/components/storefront/Header.tsx** + +```tsx +import Link from "next/link"; + +export default function Header() { +  return ( +    <header className="border-b"> +      <div className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between"> +        <Link href="/" className="text-xl font-bold">Store</Link> +        <nav className="flex gap-6"> +          <Link href="/products">Products</Link> +          <Link href="/about">About</Link> +          <Link href="/contact">Contact</Link> +          <Link href="/cart">Cart</Link> +        </nav> +      </div> +    </header> +  ); +} +``` + +- [ ] **Step 3: Create Footer src/components/storefront/Footer.tsx** + +```tsx +export default function Footer() { +  return ( +    <footer className="border-t py-8 mt-auto"> +      <div className="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500"> +        &copy; {new Date().getFullYear()} Store. All rights reserved. +      </div> +    </footer> +  ); +} +``` + +- [ ] **Step 4: Create Home page src/app/page.tsx** + +```tsx +import Link from "next/link"; +import ProductGrid from "@/components/storefront/ProductGrid"; +import { prisma } from "@/lib/prisma"; + +export default async function HomePage() { +  const products = await prisma.product.findMany({ +    where: { status: "active" }, +    take: 8, +    orderBy: { createdAt: "desc" }, +  }); + +  return ( +    <div> +      <section className="bg-gradient-to-br from-primary-50 to-white py-20"> +        <div className="max-w-7xl mx-auto px-4 text-center"> +          <h1 className="text-4xl font-bold mb-4">Welcome to Our Store</h1> +          <p className="text-lg text-gray-600 mb-8 max-w-2xl mx-auto"> +            Discover our curated collection of products. +          </p> +          <Link +            href="/products" +            className="inline-block bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-700" +          > +            Shop Now +          </Link> +        </div> +      </section> +      <section className="max-w-7xl mx-auto px-4 py-16"> +        <h2 className="text-2xl font-bold mb-8">Featured Products</h2> +        <ProductGrid products={products} /> +      </section> +    </div> +  ); +} +``` + +- [ ] **Step 5: Create ProductCard src/components/storefront/ProductCard.tsx** + +```tsx +import Link from "next/link"; + +interface ProductCardProps { +  product: { +    id: string; +    name: string; +    slug: string; +    price: number; +    images: string[]; +    category: string; +  }; +} + +export default function ProductCard({ product }: ProductCardProps) { +  return ( +    <Link +      href={`/products/${product.slug}`} +      className="group border rounded-lg overflow-hidden hover:shadow-lg transition" +    > +      <div className="aspect-square bg-gray-100"> +        {product.images[0] && ( +          <img +            src={product.images[0]} +            alt={product.name} +            className="w-full h-full object-cover group-hover:scale-105 transition" +          /> +        )} +      </div> +      <div className="p-4"> +        <p className="text-sm text-gray-500 mb-1">{product.category}</p> +        <h3 className="font-semibold">{product.name}</h3> +        <p className="text-primary-600 font-bold mt-1">${(product.price / 100).toFixed(2)}</p> +      </div> +    </Link> +  ); +} +``` + +- [ ] **Step 6: Create ProductGrid src/components/storefront/ProductGrid.tsx** + +```tsx +import ProductCard from "./ProductCard"; + +interface ProductGridProps { +  products: Array<{ +    id: string; name: string; slug: string; price: number; +    images: string[]; category: string; +  }>; +} + +export default function ProductGrid({ products }: ProductGridProps) { +  if (products.length === 0) { +    return <p className="text-gray-500">No products yet.</p>; +  } +  return ( +    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"> +      {products.map((p) => <ProductCard key={p.id} product={p} />)} +    </div> +  ); +} +``` + +- [ ] **Step 7: Verify build** + +```bash +cd D:\ecommerce-template && npx next build 2>&1 | findstr /V "Info" +``` + +- [ ] **Step 8: Commit** + +```bash +git add . && git commit -m "feat: add storefront layout, home page, product grid" +``` + +--- + + diff --git a/.superpowers/sdd/task-2-report.md b/.superpowers/sdd/task-2-report.md new file mode 100644 index 0000000..402ab8a --- /dev/null +++ b/.superpowers/sdd/task-2-report.md @@ -0,0 +1,27 @@ +# Task 2: Storefront Layout + Home Page G현 Report + +## Status: DONE + +## Files Created +- `src/app/layout.tsx` G현 Root layout with Header + Footer +- `src/app/page.tsx` G현 Home page with hero and featured products (dynamic rendering) +- `src/components/storefront/Header.tsx` G현 Site header with navigation links +- `src/components/storefront/Footer.tsx` G현 Site footer with copyright +- `src/components/storefront/ProductCard.tsx` G현 Product card component +- `src/components/storefront/ProductGrid.tsx` G현 Responsive product grid + +## Build Verification +`npx next build` G현 **PASSED** +- Compiled successfully, types valid +- Home page marked as `┕ (Dynamic)` (server-rendered on demand) +- All static pages generated without errors + +## Commits +- `e46ce15` G현 `feat: add storefront layout, home page, product grid` + +## Self-Review Findings +1. **Type fix:** Prisma returns `images` as `JsonValue`, not `string[]`. Added explicit mapping in `page.tsx` to cast `images as string[]`. +2. **Dynamic rendering:** Added `export const dynamic = "force-dynamic"` to home page so it renders at request time (prevents build failure when no database is available). + +## Concerns +- Requires `DATABASE_URL` environment variable at runtime for Prisma queries to work. diff --git a/src/app/layout.tsx b/src/app/layout.tsx new file mode 100644 index 0000000..5454733 --- /dev/null +++ b/src/app/layout.tsx @@ -0,0 +1,21 @@ +import type { Metadata } from "next"; +import "./globals.css"; +import Header from "@/components/storefront/Header"; +import Footer from "@/components/storefront/Footer"; + +export const metadata: Metadata = { +  title: "Store", +  description: "E-commerce template", +}; + +export default function RootLayout({ children }: { children: React.ReactNode }) { +  return ( +    <html lang="en"> +      <body className="min-h-screen flex flex-col"> +        <Header /> +        <main className="flex-1">{children}</main> +        <Footer /> +      </body> +    </html> +  ); +} diff --git a/src/app/page.tsx b/src/app/page.tsx new file mode 100644 index 0000000..07266ae --- /dev/null +++ b/src/app/page.tsx @@ -0,0 +1,51 @@ +import Link from "next/link"; +import ProductGrid from "@/components/storefront/ProductGrid"; +import { prisma } from "@/lib/prisma"; + +export const dynamic = "force-dynamic"; + +export default async function HomePage() { +  let products: Awaited<ReturnType<typeof prisma.product.findMany>> = []; +  try { +    const raw = await prisma.product.findMany({ +      where: { status: "active" }, +      take: 8, +      orderBy: { createdAt: "desc" }, +    }); +    products = raw; +  } catch { +    // Database unavailable G현 show empty featured section +  } + +  const safeProducts = products.map((p) => ({ +    id: p.id, +    name: p.name, +    slug: p.slug, +    price: p.price, +    images: Array.isArray(p.images) ? p.images.filter((i): i is string => typeof i === "string") : [], +    category: p.category, +  })); + +  return ( +    <div> +      <section className="bg-gradient-to-br from-primary-50 to-white py-20"> +        <div className="max-w-7xl mx-auto px-4 text-center"> +          <h1 className="text-4xl font-bold mb-4">Welcome to Our Store</h1> +          <p className="text-lg text-gray-600 mb-8 max-w-2xl mx-auto"> +            Discover our curated collection of products. +          </p> +          <Link +            href="/products" +            className="inline-block bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-700" +          > +            Shop Now +          </Link> +        </div> +      </section> +      <section className="max-w-7xl mx-auto px-4 py-16"> +        <h2 className="text-2xl font-bold mb-8">Featured Products</h2> +        <ProductGrid products={safeProducts} /> +      </section> +    </div> +  ); +} diff --git a/src/components/storefront/Footer.tsx b/src/components/storefront/Footer.tsx new file mode 100644 index 0000000..161b3f0 --- /dev/null +++ b/src/components/storefront/Footer.tsx @@ -0,0 +1,9 @@ +export default function Footer() { +  return ( +    <footer className="border-t py-8 mt-auto"> +      <div className="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500"> +        &copy; {new Date().getFullYear()} Store. All rights reserved. +      </div> +    </footer> +  ); +} diff --git a/src/components/storefront/Header.tsx b/src/components/storefront/Header.tsx new file mode 100644 index 0000000..46f5268 --- /dev/null +++ b/src/components/storefront/Header.tsx @@ -0,0 +1,17 @@ +import Link from "next/link"; + +export default function Header() { +  return ( +    <header className="border-b"> +      <div className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between"> +        <Link href="/" className="text-xl font-bold">Store</Link> +        <nav className="flex gap-6"> +          <Link href="/products">Products</Link> +          <Link href="/about">About</Link> +          <Link href="/contact">Contact</Link> +          <Link href="/cart">Cart</Link> +        </nav> +      </div> +    </header> +  ); +} diff --git a/src/components/storefront/ProductCard.tsx b/src/components/storefront/ProductCard.tsx new file mode 100644 index 0000000..1ce0e06 --- /dev/null +++ b/src/components/storefront/ProductCard.tsx @@ -0,0 +1,36 @@ +import Link from "next/link"; + +interface ProductCardProps { +  product: { +    id: string; +    name: string; +    slug: string; +    price: number; +    images: string[]; +    category: string; +  }; +} + +export default function ProductCard({ product }: ProductCardProps) { +  return ( +    <Link +      href={`/products/${product.slug}`} +      className="group border rounded-lg overflow-hidden hover:shadow-lg transition" +    > +      <div className="aspect-square bg-gray-100"> +        {product.images[0] && ( +          <img +            src={product.images[0]} +            alt={product.name} +            className="w-full h-full object-cover group-hover:scale-105 transition" +          /> +        )} +      </div> +      <div className="p-4"> +        <p className="text-sm text-gray-500 mb-1">{product.category}</p> +        <h3 className="font-semibold">{product.name}</h3> +        <p className="text-primary-600 font-bold mt-1">${(product.price / 100).toFixed(2)}</p> +      </div> +    </Link> +  ); +} diff --git a/src/components/storefront/ProductGrid.tsx b/src/components/storefront/ProductGrid.tsx new file mode 100644 index 0000000..10232c1 --- /dev/null +++ b/src/components/storefront/ProductGrid.tsx @@ -0,0 +1,19 @@ +import ProductCard from "./ProductCard"; + +interface ProductGridProps { +  products: Array<{ +    id: string; name: string; slug: string; price: number; +    images: string[]; category: string; +  }>; +} + +export default function ProductGrid({ products }: ProductGridProps) { +  if (products.length === 0) { +    return <p className="text-gray-500">No products yet.</p>; +  } +  return ( +    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"> +      {products.map((p) => <ProductCard key={p.id} product={p} />)} +    </div> +  ); +}
